<?xml version="1.0" encoding="utf-8"?>
<LinearLayout android:id="@+id/widget"
              xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="@color/white"
              android:orientation="vertical">

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="@dimen/submission_content_text_margin"
        android:textAppearance="?android:textAppearanceSmall"
        android:textColor="@android:color/primary_text_light"
        android:visibility="gone"
        android:gravity="center"/>

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:gravity="center_vertical"
        android:maxLines="2"
        android:paddingEnd="@dimen/submission_content_text_margin"
        android:paddingLeft="@dimen/submission_content_text_margin"
        android:paddingRight="@dimen/submission_content_text_margin"
        android:paddingStart="@dimen/submission_content_text_margin"
        android:textColor="@android:color/primary_text_light"
        android:textStyle="bold"
        android:visibility="gone"/>


    <ImageView
        android:id="@+id/widget_content_image"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scaleType="centerCrop"
        android:visibility="gone"/>

    <TextView
        android:id="@+id/widget_content_text"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:breakStrategy="high_quality"
        android:ellipsize="end"
        android:gravity="start"
        android:padding="@dimen/submission_content_text_margin"
        android:textAlignment="viewStart"
        android:textAppearance="?android:textAppearanceSmall"
        android:textColor="@android:color/primary_text_light"
        android:visibility="gone"/>

</LinearLayout>